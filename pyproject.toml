[tool.black]
line-length = 100
target-version = ["py311"]
exclude = '''
/(
  migrations
  | \.venv
  | build
)/
'''

[tool.isort]
profile = "black"
line_length = 100
skip = ["migrations"]

[tool.bandit]
exclude_dirs = ["tests", "migrations", ".venv"]
skips = ["B101"]
